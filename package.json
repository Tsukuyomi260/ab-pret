{
  "name": "ab-campus-finance",
  "version": "1.0.0",
  "description": "AB Campus Finance - Application de financement et d'épargne pour étudiants du Bénin - Made in Bénin",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && PORT=3001 npm start",
    "dev:backend": "cd backend && npm run dev",
    "build": "npm run build:frontend && npm run build:backend",
    "build:frontend": "cd frontend && npm run build",
    "build:backend": "cd backend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "start:frontend": "cd frontend && PORT=3001 npm start",
    "start:backend": "cd backend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "watchman": "^1.0.0",
    "web-push": "^3.6.7"
  }
}
